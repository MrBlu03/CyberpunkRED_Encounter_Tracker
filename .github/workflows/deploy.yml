name: Deploy to GitHub Pages

on:
  push:
    branches: [ main, master, V2 ]
  pull_request:
    branches: [ main, master, V2 ]

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: gm-tool/package-lock.json
      
      - name: Install dependencies
        run: |
          cd gm-tool
          npm ci
      
      - name: Build
        run: |
          cd gm-tool
          npm run build
          cd ..
          mkdir -p deploy/gm-tool
          cp -r gm-tool/dist/* deploy/gm-tool/
          cp -r gm-tool/dist/. deploy/gm-tool/
          cp index.html deploy/
          echo "" > deploy/.nojekyll
          ls -la deploy/
          ls -la deploy/gm-tool/
          cat deploy/gm-tool/index.html
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: deploy
          retention-days: 1

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' || github.ref == 'refs/heads/V2'
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
